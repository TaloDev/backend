version: '3.9'

services:
  backend:
    build:
      context: .
      target: dev
    image: backend
    volumes:
      - ./src:/usr/backend/src
      - ./tests:/usr/backend/tests
    depends_on:
      - db
